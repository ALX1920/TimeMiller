services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DJANGO_DB_PATH=/app/data/db.sqlite3
    volumes:
      - visits_data:/app/data

volumes:
  visits_data:
